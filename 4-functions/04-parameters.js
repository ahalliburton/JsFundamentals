//Parameters

function hi(name) {
    console.log(`Hi ${name}!`); //string interpolation
}

hi('John');

1. the Parameters we need to take into the function
2. Usin string interpolation, we can refer to the parameter we passed
3. we define what the parameters value will be

function pet(animal) {
    console.log(`my pets name is ${animal}!`);
    }

    pet('Georgie');
    pet('Luna');
    pet('finn');

function name(lava, cream) {
    console.log(`Hello, my name is ${lava} ${cream}`);
}

name('Allan', 'Halliburton');