//*literals

//*javascript defines two types of values; fixed and variable
//fixed are literals-hard coded
//variables are variables

let days = ['Monday', 'Tuesday', 'Wednesday'] 
console.log(days.toString());
console.log(days.length);

let tired = true;

let soup = {
    a: 'chicken noodle',
    b: 'tomato',
    c: 'beef and barley'
}
console.log(soup.a);

let car = 'Honda';

let dec = 10.2;